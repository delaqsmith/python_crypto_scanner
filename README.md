# python_crypto_scanner
This project is an automated Bitcoin price tracker that uses Python to scrape the current Bitcoin price from CoinMarketCap. The program records the cryptocurrency name, price, and timestamp in a CSV file and automatically updates the data every 10 seconds.
